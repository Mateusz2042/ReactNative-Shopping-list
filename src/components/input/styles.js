import { Colors } from '../../styles/colors';

export default {
  input: {
    borderWidth: 1,
    borderColor: Colors.black,
    borderRadius: 12,
    backgroundColor: Colors.semiLightOrange,
    fontWeight: 'bold',
    fontSize: 15,
    color: Colors.grey,
  },
};
